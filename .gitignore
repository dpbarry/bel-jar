.idea/
.claude/
.cursor/
node_modules/

# Accidental / local dumps (not product code — see docs/CODEMAP.md)
3/
scratch/*
!scratch/README.md

# Submodule build cache (noise in parent status)
Beluga-W/_build/

# Corpus masking-harness run outputs (regenerate via `npm run corpus`/`heldout`)
results/corpus/

_debug-*.mjs
tests/_scratch-*.mjs

# One-off local copies (Beluga site hosts the real PDFs)
tutorial.pdf
userguide.pdf

# Prover session scratch (regenerate locally)
scripts/debug-wtp-*.mjs
scripts/debug-beta*.mjs
scripts/tmp-*.mjs
scripts/.str-wtp-*
scripts/.shots/
scripts/*.log

# Prover debug probes + local fixtures (not product)
scripts/debug-*.mjs
scripts/.str-step-*.bel
