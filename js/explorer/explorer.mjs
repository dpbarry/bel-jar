/**
 * Explorer domain graph — one shell build entry.
 * Suite layout ships with workspace (imports project-source).
 * Explorer search lives in the library graph (needs LibrarySearch).
 */
import './explorer-inline-name.mjs';
import './explorer-tree.mjs';
