/**
 * Dialogs leaf graph — one shell build entry.
 * Real imports between dialog → prompt → confirm / name-prompt / conflict.
 * Assigns Dialog, PromptDialog, ConfirmDialog, NamePrompt, ConflictDialog.
 */
import './dialog.mjs';
import './prompt-dialog.mjs';
import './confirm-dialog.mjs';
import './name-prompt.mjs';
import './conflict-dialog.mjs';
